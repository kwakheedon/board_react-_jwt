node_modules

# .gitignore에 추가
echo 'backend/.metadata/' >> .gitignore

# Git 캐시에서 제거
git rm -r --cached backend/.metadata/

# 변경 반영
git add .gitignore
git commit -m "chore: .metadata 폴더 gitignore 처리"

git config --global core.autocrlf true

# Eclipse IDE 설정 및 캐시 무시
.metadata/
*.log
*.swp
*.tmp
*.class

# Eclipse metadata
backend/.metadata/

# application-secret.properties 또는 application-dev.properties 와 같은
# 민감한 정보를 담은 설정 파일은 Git에 올리지 않도록 제외합니다.
application-*.properties

echo ".metadata/" >> backend/.gitignore