# Eclipse IDE 설정 및 캐시 무시
.metadata/
*.log
*.swp
*.tmp
*.class

# Eclipse metadata
backend/.metadata/

# application-secret.properties 또는 application-dev.properties 와 같은
# 민감한 정보를 담은 설정 파일은 Git에 올리지 않도록 제외합니다.
application-*.properties

echo ".metadata/" >> backend/.gitignore